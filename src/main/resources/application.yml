server:
  port: 8080
spring:
  datasource:
    password: ''
    url: jdbc:mysql://localhost:3306/bd_projectsport_cb
    username: root
  jpa:
    database-platform: org.hibernate.dialect.MySQL5Dialect
    generate-ddl: true
    hibernate:
      ddl-auto: create
